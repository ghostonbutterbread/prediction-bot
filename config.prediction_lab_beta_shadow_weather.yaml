# Weather lane beta-shadow Prediction Lab observer runtime.
# Stable config stays in config.yaml; this file composes beta-shadow
# observability with isolated Prediction Lab runtime/output paths.
config_composition:
  base: config.yaml
  overlays:
    - beta_shadow_observability_all
    - prediction_lab_beta_shadow_runtime
