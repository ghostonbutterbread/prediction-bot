{
  "name": "prediction-bot-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only local dashboard for Prediction Bot lane status and PnL.",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
