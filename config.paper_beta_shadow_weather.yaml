# Weather lane beta-shadow paper runtime.
# Stable config stays in config.yaml; this file composes beta-shadow
# observability with isolated paper runtime/output paths.
config_composition:
  base: config.yaml
  overlays:
    - beta_shadow_observability_all
    - paper_beta_shadow_runtime
